import React, { Component } from 'react';

export default class App extends Component {
  state = {
    photo_id : []
  }

  render() {
    return (
      <div pathname={'test'}>We Have a Component Now! Exciting stuff.</div>
    );
  }
}
